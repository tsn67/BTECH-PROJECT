# PR

- Create a separate .gitignore inside your application source folders (only if required).
- For any new implementation, fork the repository (or pull the latest changes into your existing fork), create a new branch, and apply your changes there.
- After completing your changes, create a Pull Request to the original repository.

<Commit>

# Make all commits in the following format:

Commit Title
Implementations:
  - implementation 1
  - implementation 2
Fixes:
  - fix 1
  - fix 2
